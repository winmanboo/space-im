spring:
  application:
    name: im-router-core
  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: localhost:8848
      config:
        import-check:
          enabled: false
        file-extension: yaml
        extension-configs:
          - data-id: im-router-core.yaml
            group: DEFAULT_GROUP
            refresh: true