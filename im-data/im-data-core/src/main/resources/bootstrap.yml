spring:
  application:
    name: im-data-core
  cloud:
    nacos:
      username: nacos
      password: nacos
      server-addr: localhost:8848
      config:
        import-check:
          enabled: false
        file-extension: yaml
        extension-configs:
          - data-id: im-data-core.yaml
            group: DEFAULT_GROUP
            refresh: true